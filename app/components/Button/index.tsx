import type { NextPage } from 'next'

const Button: NextPage = () => {
  return (
    <button className="uppercase font-semibold text-sm px-12 py-4 rounded-md bg-white border-gray-200 hover:border-gray-500 border-2">
      LOAD MORE
    </button>
  )
}

export default Button
